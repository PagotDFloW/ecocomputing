
{% extends 'base.html.twig' %}

{% block title %}Hello AvisController!{% endblock %}

{% block body %}
<style>
    .singleavis { border: 2px; border-color: blue; margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<div class="container">
    <h1>Vos Avis</h1>
    {% if (avis|length > 0) %}
    <ul class="singleavis">
        {% for singleavis in avis %}
            <li>
            <a>Avis n° {{ singleavis.id }} :</a>
            <a>{{ singleavis.avis }}</a>
            </li>
        {% endfor %}
    </ul>
    {% else %}
        <p>Il n'y a pas de categories pour le moment</p>
    {% endif %}
</div>
{% endblock %}
