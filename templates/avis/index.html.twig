{% extends 'base.html.twig' %}

{% block title %}Hello AvisController!{% endblock %}

{% block body %}
<style>
    
</style>
<div class="container avis">
    <h1 class="titleindexavis">Vos Avis</h1>
    {% if (avis|length > 0) %}
        {% for singleavis in avis %}
            <div class="singleavis">
            <p class="avisid">
            <a href="{{ path('singleAvis', {id: singleavis.id} )}}"><i class="fas fa-eye"></i></a>
            
            Avis n° {{ singleavis.id }} :</p>
            <p>{{ singleavis.avis }}</p>
            </div>
        {% endfor %}
    {% else %}
        <p>Il n'y a pas de categories pour le moment</p>
    {% endif %}
</div>
{% endblock %}