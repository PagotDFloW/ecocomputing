{% extends "base.html.twig" %}


{% block body %}

    <div class="container">
        <div class="col-10 m-auto d-flex flex-column align-items-start justify-content-between flex-wrap mt-5">
            <div class="col-12 d-flex align-items-center p-3 mb-4 shadow-sm" style="border: 1px solid #ccc; border-radius:10px;">
                <div class="col-12 d-flex flex-column">
                    <div class="col-12 d-flex flex-column align-items-center p-3 mb-4 shadow-sm" style="border: 1px solid #ccc; border-radius:10px;">
                        {{ form_start(form, {'attr': {'class': 'col-12'}}) }}
                            <div class="col-12 d-flex justify-content-around">
                                <div class="col-8">
                                    <div class="d-flex flex-column">
                                        <p style="font-weight:bold; margin-bottom:1rem !important;">{{ service.name }}</p>
                                        <small>{{ service.description }}</small>
                                        <small>Prix du service : {{ service.price }}€</small>
                                    </div>
                                </div>
            
                                <div class="col-4">
                                    <div class="d-flex justify-content-center align-items-center">
                                        {{ form_row(form.computer) }}
                                    </div>
                                </div>
                            </div>

                            <div class="d-flex flex-column mt-4">
                                <button type="submit" class="col-6 m-auto btn btn-success">Valider la demande</button>
                            </div>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}